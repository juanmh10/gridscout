"""Read-only browser worker for opt-in OLX access.

The WSL launcher connects this worker to a normal visible Chrome profile over
CDP. A container-local browser remains available when no CDP endpoint is set.
"""
